# AgentApiTypescript

Note: In Agent file param types has been added using auto syntax using prettier and eslint, need to change param types when we will use the corresponding methods for the api call.
